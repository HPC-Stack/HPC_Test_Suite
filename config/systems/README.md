# System Configurations

Each YAML file defines an HPC system's partitions, schedulers, and environments.

Generated by `continus-bench add-system` or created manually.

To add a new system, run:

```bash
python3 -m continuousbench.cli.main add-system
```
